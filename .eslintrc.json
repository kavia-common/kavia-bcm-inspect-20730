{
  "extends": [
    "react-app",
    "react-app/jest"
  ],
  "rules": {
    // Add any project-specific rules here, but do not re-declare react-hooks plugin rules.
    // Example overrides (kept minimal to avoid conflicts):
    // "no-console": "warn"
  },
  "overrides": [
    {
      "files": ["**/*.ts", "**/*.tsx"],
      "rules": {
        // TypeScript-specific overrides can go here if needed.
      }
    }
  ]
}
